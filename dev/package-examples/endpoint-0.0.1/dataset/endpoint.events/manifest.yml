title: Endpoint events

# TODO currently only metrics and logs are supported
type: events

# If set to true, this will be enabled by default in the input selection
default: true
# TODO do we have any variables?
#vars:

