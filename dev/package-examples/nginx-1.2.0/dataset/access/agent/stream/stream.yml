# This is not an array on purpose to make sure only 1 single input is specified in this file.

type: log
paths:
{{#each paths}}
  - "{{this}}"
{{/each}}

exclude_files: [".gz$"]

processors:
  - add_locale: ~
