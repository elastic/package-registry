title: aws natgateway metrics
release: beta
type: metrics
streams:
- input: aws/metrics
  vars:
  - name: period
    type: text
    title: Period
    multi: false
    required: true
    show_user: true
    default: 1m
  - name: tags_filter
    type: yaml
    title: Tags Filter
    multi: false
    required: true
    show_user: true
    default: |
      - key: dept
        value: eng
  title: aws natgateway metrics
  description: Collect aws natgateway metrics
