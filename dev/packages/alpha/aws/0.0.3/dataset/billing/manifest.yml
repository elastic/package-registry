title: aws billing metrics
release: beta
type: metrics
streams:
- input: aws/metrics
  vars:
  - name: period
    type: text
    title: Period
    multi: false
    required: true
    show_user: true
    default: 12h
  title: aws billing metrics
  description: Collect aws billing metrics
