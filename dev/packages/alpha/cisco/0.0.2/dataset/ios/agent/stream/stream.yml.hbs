paths:
{{#each paths}}
  - {{this}}
{{/each}}
exclude_files: [".gz$"]
tags: {{this.tags}}
processors:
  - add_locale: ~
  - script:
      lang: javascript
      id: cisco_ios
      source: >
{{ file("pipeline.js") | indent(8) }}
