protocol.udp:
  host: "{{syslog_host}}:{{syslog_port}}"
tags: {{tags}}
processors:
  - add_locale: ~
  - script:
      lang: javascript
      id: cisco_ios
      source: >
{{ file("pipeline.js") | indent(8) }}

