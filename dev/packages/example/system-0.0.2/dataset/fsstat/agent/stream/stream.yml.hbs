input: system/metrics
metricset: fsstat
period: {{period}}
dataset: system.fsstat
filesystem.ignore_types: {{ignore_types}}

processors:
  - drop_event.when.regexp:
      system.filesystem.mount_point: '^/(sys|cgroup|proc|dev|etc|host|lib|snap)($|/)'
