metricsets: ["stubstatus"]
period: {{period}}

# Nginx hosts
hosts:
{{#each hosts}}
  - {{this}}
{{/each}}

# Path to server status. Default server-status
#server_status_path: "server-status"

{{#if username}}
username: "{{username}}"
{{/if}}
{{#if password}}
password: "{{password}}"
{{/if}}
