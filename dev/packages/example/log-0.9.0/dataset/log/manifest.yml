title: Log Dataset

type: logs
ingest_pipeline: default

default: true

inputs:
  - type: log
    vars:
      - name: paths
        required: true
        description: Path to log files to be collected
        type: text
        multi: true

