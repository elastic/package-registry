title: AWS ebs metrics
release: ga
type: metrics

streams:
  - input: aws/metrics
    title: AWS ebs metrics
    description: AWS ebs metrics
    vars:
      - name: period
        description: Collection frequency for ebs metrics.
        type: period
        default: 5m
