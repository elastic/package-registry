title: AWS elb metrics
release: ga
type: metrics

streams:
  - input: aws/metrics
    title: AWS elb metrics
    description: AWS elb metrics
    vars:
      - name: period
        description: Collection frequency for elb metrics.
        type: period
        default: 1m
