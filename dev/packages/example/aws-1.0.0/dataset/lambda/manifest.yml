title: AWS lambda metrics
release: beta
type: metrics

streams:
  - input: aws/metrics
    title: AWS lambda metrics
    description: AWS lambda metrics
    vars:
      - name: period
        description: Collection frequency for lambda metrics.
        type: period
        default: 5m
