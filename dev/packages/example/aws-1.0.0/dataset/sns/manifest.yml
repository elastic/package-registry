title: AWS sns metrics
release: ga
type: metrics

streams:
  - input: aws/metrics
    title: AWS sns metrics
    description: AWS sns metrics
    vars:
      - name: period
        description: Collection frequency for sns metrics.
        type: period
        default: 5m
