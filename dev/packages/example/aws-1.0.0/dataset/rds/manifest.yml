title: AWS rds metrics
release: ga
type: metrics

streams:
  - input: aws/metrics
    title: AWS rds metrics
    description: AWS rds metrics
    vars:
      - name: period
        description: Collection frequency for rds metrics.
        type: period
        default: 5m
