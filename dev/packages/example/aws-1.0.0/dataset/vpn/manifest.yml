title: AWS vpn metrics
release: beta
type: metrics

streams:
  - input: aws/metrics
    title: AWS vpn metrics
    description: AWS vpn metrics
    enabled: false
    vars:
      - name: period
        description: Collection frequency for vpn metrics.
        type: period
        default: 1m
