id: stats
title: beat stats metrics
release: ga
type: metrics
streams:
- input: beat/metrics
  vars:
  - default:
    - http://localhost:5066
    name: hosts
  - default: 10s
    name: period
  - default: true
    name: xpack.enabled
  title: beat stats logs
  description: Collect beat stats metrics
