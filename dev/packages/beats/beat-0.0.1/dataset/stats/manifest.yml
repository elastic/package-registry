title: Beat stats metrics
release: ga
type: metrics
streams:
- input: beat/metrics
  vars:
  - default:
    - http://localhost:5066
    name: hosts
  - default: 10s
    name: period
  - default: true
    name: xpack.enabled
  title: Beat stats metrics
  description: Collect Beat stats metrics
