title: Stan channels metrics
release: ga
type: metrics
streams:
- input: stan/metrics
  vars:
  - default:
    - localhost:8222
    multi: true
    name: hosts
  - default: 60s
    name: period
  title: Stan channels metrics
  description: Collect Stan channels metrics
