id: channels
title: stan channels metrics
release: ga
type: metrics
streams:
- input: stan/metrics
  vars:
  - default:
    - localhost:8222
    name: hosts
  - default: 60s
    name: period
  title: stan channels logs
  description: Collect stan channels metrics
