title: Stan stats metrics
release: ga
type: metrics
streams:
- input: stan/metrics
  vars:
  - default:
    - localhost:8222
    name: hosts
  - default: 60s
    name: period
  title: Stan stats metrics
  description: Collect Stan stats metrics
