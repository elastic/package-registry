title: kibana log logs
release: ga
type: logs
streams:
- input: logs
  vars:
  - name: paths
    type: text
    title: Paths
    multi: true
    required: true
    show_user: true
    default:
    - /var/log/kibana/kibana.stdout
  title: kibana log logs
  description: Collect kibana log logs
