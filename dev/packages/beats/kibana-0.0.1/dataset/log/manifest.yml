title: kibana log logs
release: ga
type: logs
streams:
- input: logs
  vars:
  - default:
    - /var/log/kibana/kibana.stdout
    multi: true
    name: paths
    required: true
    show_user: true
    title: Paths
    type: text
  title: kibana log logs
  description: Collect kibana log logs
