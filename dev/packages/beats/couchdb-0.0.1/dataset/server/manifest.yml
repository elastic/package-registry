title: CouchDB server metrics
release: ga
type: metrics
streams:
- input: couchdb/metrics
  vars:
  - default:
    - localhost:5984
    name: hosts
  - default: 10s
    name: period
  title: CouchDB server metrics
  description: Collect CouchDB server metrics
