title: CouchDB server metrics
release: ga
type: metrics
streams:
- input: couchdb/metrics
  vars:
  - default:
    - localhost:5984
    multi: true
    name: hosts
    required: true
    show_user: true
    title: Hosts
  - default: 10s
    multi: false
    name: period
    required: true
    show_user: true
    title: Period
  title: CouchDB server metrics
  description: Collect CouchDB server metrics
