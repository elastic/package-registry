title: aws ec2 metrics
release: ga
type: metrics
streams:
- input: aws/metrics
  vars:
  - default: test-mb
    name: credential_profile_name
  - default: 300s
    name: period
  - default:
    - key: Organization
      value: Engineering
    name: tags_filter
  title: aws ec2 metrics
  description: Collect aws ec2 metrics
