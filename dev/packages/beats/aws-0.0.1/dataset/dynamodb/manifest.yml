title: aws dynamodb metrics
release: beta
type: metrics
streams:
- input: aws/metrics
  vars:
  - default: 5m
    multi: false
    name: period
    required: true
    show_user: true
    title: Period
  title: aws dynamodb metrics
  description: Collect aws dynamodb metrics
