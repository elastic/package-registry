id: dynamodb
title: aws dynamodb metrics
release: beta
type: metrics
streams:
- input: aws/metrics
  vars:
  - default: 5m
    name: period
  title: aws dynamodb metrics
  description: Collect aws dynamodb metrics
