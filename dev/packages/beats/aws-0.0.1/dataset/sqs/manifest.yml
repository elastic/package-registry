title: aws sqs metrics
release: ga
type: metrics
streams:
- input: aws/metrics
  vars:
  - default: test-mb
    name: credential_profile_name
  - default: 300s
    name: period
  - default:
    - us-west-1
    name: regions
  title: aws sqs metrics
  description: Collect aws sqs metrics
