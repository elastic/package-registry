id: lambda
title: aws lambda metrics
release: beta
type: metrics
streams:
- input: aws/metrics
  vars:
  - default: 5m
    name: period
  title: aws lambda metrics
  description: Collect aws lambda metrics
