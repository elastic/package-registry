title: aws lambda metrics
release: beta
type: metrics
streams:
- input: aws/metrics
  vars:
  - default: 5m
    multi: false
    name: period
    required: true
    show_user: true
    title: Period
  title: aws lambda metrics
  description: Collect aws lambda metrics
