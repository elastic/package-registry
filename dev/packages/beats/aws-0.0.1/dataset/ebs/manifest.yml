title: aws ebs metrics
release: ga
type: metrics
streams:
- input: aws/metrics
  vars:
  - default: 5m
    multi: false
    name: period
    required: true
    show_user: true
    title: Period
    type: text
  title: aws ebs metrics
  description: Collect aws ebs metrics
