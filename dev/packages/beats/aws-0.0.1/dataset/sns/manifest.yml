title: aws sns metrics
release: beta
type: metrics
streams:
- input: aws/metrics
  vars:
  - default: 5m
    multi: false
    name: period
    required: true
    show_user: true
    title: Period
    type: text
  title: aws sns metrics
  description: Collect aws sns metrics
