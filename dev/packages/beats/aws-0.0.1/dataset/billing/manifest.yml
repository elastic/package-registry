title: aws billing metrics
release: beta
type: metrics
streams:
- input: aws/metrics
  vars:
  - default: 12h
    name: period
  - default:
    - us-east-1
    name: regions
  title: aws billing metrics
  description: Collect aws billing metrics
