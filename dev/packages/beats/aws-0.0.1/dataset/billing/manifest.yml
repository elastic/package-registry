title: aws billing metrics
release: beta
type: metrics
streams:
- input: aws/metrics
  vars:
  - default: 12h
    multi: false
    name: period
    required: true
    show_user: true
    title: Period
  - default:
    - us-east-1
    multi: true
    name: regions
    required: true
    show_user: true
    title: Regions
  title: aws billing metrics
  description: Collect aws billing metrics
