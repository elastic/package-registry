title: Windows service metrics
release: ga
type: metrics
streams:
- input: windows/metrics
  vars:
  - default: 60s
    multi: false
    name: period
    required: true
    show_user: true
    title: Period
    type: text
  title: Windows service metrics
  description: Collect Windows service metrics
