title: MongoDB replstatus metrics
release: ga
type: metrics
streams:
- input: mongodb/metrics
  vars:
  - default:
    - localhost:27017
    multi: true
    name: hosts
  - default: 10s
    name: period
  title: MongoDB replstatus metrics
  description: Collect MongoDB replstatus metrics
