title: MongoDB metrics metrics
release: ga
type: metrics
streams:
- input: mongodb/metrics
  vars:
  - default:
    - localhost:27017
    name: hosts
  - default: 10s
    name: period
  title: MongoDB metrics metrics
  description: Collect MongoDB metrics metrics
