title: Ceph pool_disk metrics
release: ga
type: metrics
streams:
- input: ceph/metrics
  vars:
  - default:
    - localhost:5000
    multi: true
    name: hosts
  - default: 10s
    name: period
  title: Ceph pool_disk metrics
  description: Collect Ceph pool_disk metrics
