id: store
title: etcd store metrics
release: ga
type: metrics
streams:
- input: etcd/metrics
  vars:
  - default:
    - localhost:2379
    name: hosts
  - default: 10s
    name: period
  title: etcd store logs
  description: Collect etcd store metrics
