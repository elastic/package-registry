title: System syslog logs
release: ga
type: logs
streams:
- input: logs
  vars:
  - name: paths
    type: text
    title: Paths
    multi: true
    required: true
    show_user: true
    default:
    - /var/log/messages*
    - /var/log/syslog*
    os:
      darwin:
        default:
        - /var/log/system.log*
      windows:
        default: []
  template_path: syslog.yml
  title: System syslog logs using log
  description: Collect System syslog logs using log input
