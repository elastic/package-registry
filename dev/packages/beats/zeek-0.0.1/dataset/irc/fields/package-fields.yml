- description: |
    Fields from Zeek/Bro logs after normalization
  fields:
  - description: |
      A unique identifier of the session
    name: session_id
    type: keyword
  name: zeek
  type: group
