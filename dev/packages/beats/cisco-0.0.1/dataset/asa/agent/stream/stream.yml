{{#if input == syslog}}
input: udp
udp:
host: "{{syslog_host}}:{{syslog_port}}"
{{else if input == file}}
input: log
paths:
{{#each paths}}
  - {{this}}
{{/each}}
exclude_files: [".gz$"]
{{/if}}
tags: {{tags}}
processors:
  - add_locale: ~
