title: Redis keyspace metrics
release: ga
type: metrics
streams:
- input: redis/metrics
  vars:
  - default:
    - 127.0.0.1:6379
    multi: true
    name: hosts
  - default: 10s
    name: period
  title: Redis keyspace metrics
  description: Collect Redis keyspace metrics
