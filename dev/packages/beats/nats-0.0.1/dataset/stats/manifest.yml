title: Nats stats metrics
release: ga
type: metrics
streams:
- input: nats/metrics
  vars:
  - default:
    - localhost:8222
    multi: true
    name: hosts
  - default: 10s
    name: period
  title: Nats stats metrics
  description: Collect Nats stats metrics
