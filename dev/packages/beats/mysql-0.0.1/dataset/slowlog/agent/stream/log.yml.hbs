type: log
paths:
{{#each paths as |path|}}
 - {{path}}
{{/each}}
exclude_files: ['.gz$']
multiline:
  pattern: '^(# User@Host: |# Time: )'
  negate: true
  match: after
exclude_lines: ['^[\/\w\.]+, Version: .* started with:.*', '^# Time:.*']   # Exclude the header and time