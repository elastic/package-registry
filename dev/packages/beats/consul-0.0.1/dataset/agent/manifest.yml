title: consul agent metrics
release: beta
type: metrics
streams:
- input: consul/metrics
  vars:
  - default:
    - localhost:8500
    multi: true
    name: hosts
  - default: 10s
    name: period
  title: consul agent metrics
  description: Collect consul agent metrics
