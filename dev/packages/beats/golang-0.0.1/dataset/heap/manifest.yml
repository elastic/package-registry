id: heap
title: golang heap metrics
release: ga
type: metrics
streams:
- input: golang/metrics
  vars:
  - default: /debug/vars
    name: heap.path
  title: golang heap logs
  description: Collect golang heap metrics
